# 세션 로그 — 2026-08-06 (T-98 확장 E2E 자동화 + chrome-devtools-mcp)

## 1. 무엇을 (T-98 + MCP 설정)
- **chrome-devtools-mcp 설치/설정**: `~/.config/opencode/opencode.jsonc`에 MCP 등록 (v1.6.0). Chrome 안정판이 없어 `--executablePath`를 Whale로 지정, 네이버 봇 감지 우회 위해 `--userDataDir` 기존 `.whale-profile` 재사용. 실검증: 스마트스토어 JSON-LD 69,800원 추출 성공. opencode 재시작 후 MCP 도구 사용 가능.
- **확장 E2E 자동화 (T-98)**: 로컬 서버 격리 + 실제 Whale + 실제 확장으로 전체 파이프라인(가격 추출 → 서버 저장 → 팝업 표시) 자동 검증
  - `scripts/e2e/run-e2e.sh`: 서버 기동(`/health` 대기) → e2e.js → 서버 종료 (trap + exec로 PID 정상 종료 보장)
  - `scripts/e2e/e2e.js`: 확장 복사본 생성(`common.js` 서버 주소 → 로컬 포트 치환, 원본 불변) → 데모 데이터 주입(핫딜 5개) → Whale+확장 로드(`.whale-profile` 복사 재사용) → 상품 페이지 방문 → 가격 추출/저장 대기 → 팝업 렌더링 검증 → 데모 정리(404 확인)
  - `scripts/e2e/README.md`: 실행법 + 몰별 주의사항 + 에러코드
  - `scripts/e2e/package.json`: playwright-core ^1.62.1

## 2. 플랫폼
- extension + server (E2E), opencode 설정 (MCP)

## 3. 빌드/검증 결과
- **연속 3회 실행 10/10 통과** (TC-E2E-001~006: /health, 확장 로드, 상품 저장, 가격 이력, 팝업 상품명/가격/통계, 핫딜 5개, 데모 정리)
- 팝업 검증 실제값: `[조유리 PICK/트러블1등] 셀라딕스 트러블 세범 리밸런싱 131 앰플 30ml` / `25,400원` / `평균 25,400원 · 최저 25,400원` / 역대 최저 배지 / 핫딜 5개
- 서버 종료·임시 DB·프로필·복사본 자동 정리 확인 (연속 실행 재현성 통과)
- JS 문법 `node --check`, 셸 문법 `bash -n` 통과

## 4. 실측 이슈 (중요)
- **네이버 스마트스토어**: 자동화 브라우저 접속 시 **HTTP 429 봇 차단** (프로필 복사본/원본 모두). 수십 분~수 시간 후 재시도 필요. → E2E 기본 상품을 **올리브영**(봇 차단 없음)으로 변경
- **쿠팡**: 기존 알려진 403/봇 차단
- 포트 8000은 타 프로젝트(SmartSeller)가 점유 → E2E 전용 포트 **8765** 사용
- `run-e2e.sh` 초기 버그: `(cd && uvicorn) &` 서브셸로 띄우면 `$!`가 서브셸 PID → uvicorn 잔존. `(cd && exec env ... uvicorn) &`로 수정 → 정상 종료

## 5. 남은 TODO (T-번호)
- T-96a 잔여: 사용자가 PNG 품질 확인 + 스토어 해상도 리사이즈
- T-96b/c/d: 웨일 스토어 등록 절차 (사용자)
- T-97: Chrome Web Store ($5, 보류)
- (선택) E2E에 네이버 429 재시도 로직 또는 몰별 다중 URL 검증 추가

## 6. 문서 업데이트 목록
- `docs/plans/PLAN_v0.10.6_e2e.md` (신규), `docs/TODO.md` (T-98 완료), `docs/CHANGELOG.md` (v0.10.6), `scripts/e2e/README.md` (신규), `AGENTS.local.md` (검증 워크플로우에 E2E 추가), `.gitignore` (scripts/e2e/node_modules/, .e2e-server.log)

## 7. 오프라인 큐 상태
- 해당 없음 (이 프로젝트는 오프라인 큐 미구현 — v2.1 표준은 미적용)

## 8. E2E/k6 결과
- E2E: 연속 3회 10/10 통과. k6 부하 테스트는 미실시 (필요 시 `scripts/load-test.js` 표준안 참고)

## 9. 다음 에이전트 전달사항
- E2E 실행: `./scripts/e2e/run-e2e.sh` (기본 올리브영, 상품 지정은 인자로)
- 네이버 429 주의 — 몰 검증은 올리브영 우선
- chrome-devtools-mcp는 opencode 재시작 후 `opencode mcp list`로 연결 확인, 확장 팝업/DOM 디버깅에 활용
