# 세션 로그 — 2026-08-08

## 1. 무엇을 (T-번호)
- **T-105 (v0.12.3)** 공개 핫딜 피드 완료 — 다사자 분석 반영. 확장 "전체 핫딜" 탭 + FAB 뷰어 전환.
  - 서버: `GET /api/v1/deals/public` (익명 집계 + watchers + 5분 인메모리 캐시)
  - 팝업: 탭 "전체 핫딜" + `/deals/public` + `👀 N명이 찜` 배지(0명 포함) + 핫딜 리스트 내부 스크롤(`max-height:320px`)
  - FAB 뷰어: "오늘의 핫딜"→"전체 핫딜", 공개 파드 전환 + 찜 배지 + 메뉴 재배치(전체 핫딜→가격 추이 위)
  - 콘텐츠 스크립트 로그 `badge top 12px` 제거

## 2. 플랫폼
- server (FastAPI, recommendations.py) / extension (popup.js·popup.html·popup.css·swb-ui.js·content.js·manifest.json)

## 3. 빌드 결과 + PERF + CACHE
- pytest **44건 통과** (회귀 + 공개 파드 3종)
- `node --check` popup.js / swb-ui.js / content.js 통과
- 미리보기 스크롤 검증: deal-list scrollHeight 706 > clientHeight 320, body scrollHeight 600 유지 → FULL 데이터에도 팝업 600px 고정
- 캐시: `_DEAL_CACHE` 5분 TTL 동작 확인 (테스트에서 동일 키 1개 유지; conftest `_DEAL_CACHE.clear()`로 테스트 격리)

## 4. 남은 TODO
- **운영 배포**: Render에 서버 재배포 → `/deals/public` 활성화
- **사용자 웨일 재확인**: 확장 재로드 후 팩업/FAB 렌더 + watchers 배지 (초기엔 대부분 0)
- **웨일 스토어 재심사**: v0.12.3 배포 위해 업로드 필요
- Chrome 스토어 병행 등록(T-xx, 사용자 계정 필요) — 미진행

## 5. 다음 에이전트 전달 로그
- `/deals/public`은 `_IncludedRouter`로 포함돼 path 미노출 → **실동작은 서버 기동으로만 확인 가능**. 운영(Render) 재배포 후 `curl /api/v1/deals/public?limit=3` 로 200 확인 필
- 팝업·FAB 모두 `SWB_CONFIG.server`(common.js) = 운영 `https://shop-wisebar.onrender.com` 기준. 로컬 확인 시 base 교체 필요(원점 복구)
- DevTools 스크립트 웨일 인스턴스는 사용자 웨일 프로필과 별개 → 확장이 안 보임. 실제 검증은 사용자 웨일에서.

## 6. 문서 업데이트 목록
- `docs/TODO.md` (T-105 ✅ 완료), `docs/CHANGELOG.md` (v0.12.3, FAB 재배치/로그 제거 추가), `docs/api/ENDPOINTS.md` (`/deals/public` 명세) — 모두 반영

## 7. 오프라인 큐 상태
- 해당 없음 (오프라인 큐 미적용 — T-105는 단순 GET 피드)

## 8. E2E/k6 결과
- 해당 없음 (확장/서버 API 단위 → 44건 pytest로 대체)
---

## v0.15.0 (T-115) 추가 세션 — macOS 앱 "똑바 매니저 (ShopWiseBar Manager)" (2026-08-08)

1. **무엇을**: 
- T-115a 서버 `/admin/*` 5종(overview/trend/malls/collect/insight) + test_admin.py 6건 → pytest 66건 통과
- T-115b~d macOS 앱 `macos/ShopWiseBarManager`: xcodegen project.yml, DesignSystem(확장 토큰 기반), APIClient(@Observable AppModel, 운영/로컬 서버 토글), Music스타일 NavigationSplitView(대시보드/인사이트/통계/수집/공통 핫딜)
- 표시명: **똑바 매니저 (ShopWiseBar Manager)** — CFBundleDisplayName + 창 제목 + 사이드바 확인. 폴터는 영어 유지(ShopWiseBarManager)
2. **플랫폼**: server (admin.py) + macos (SwiftUI, Xcode 26.6/Swift 6.0)
3. **빌드/검증**: xcodebuild Debug BUILD SUCCEEDED. 앱 실행 → 로컬 서버로 6개 API 호출 확인 + 접근성 덤프로 대시보드 카드(613/3/414/477/0/3/4/379/0) 렌더 확인. 스크린샷 docs/screenshots/macos/v0.15.0_dashboard.png
4. **남은 TODO**: 운영(Render) 서버 재배포로 `/admin/*` 배포 + 앱 기본 서버(운영)에서 실데이터 확인. T-115e 상품 상세 드릴다운(데이터 보유 시 후속). macOS 빌드 재실은 `xcodegen generate` 후 `xcodebuild -derivedDataPath build/DerivedData`
5. **다음 에이전트 전달**: `/admin/*`는 운영에 아직 배포 전 → Render 재배포 필요. 앱 기본 서버는 `https://shop-wisebar.onrender.com`(APIClient.swift). 로컬 테스트 시 UserDefaults `admin.server.override` 설정 후 재실행(끝나면 삭제). xcodeproj는 gitignore(재생성 필수)
6. **문서**: PLAN_v0.15.0_admin-macos.md / TODO T-115(진행) / CHANGELOG v0.15.0 / ENDPOINTS.md admin섹션
7. **오프라인 큐**: 해당 없음
8. **E2E/k6**: 해당 없음 (macos 앱은 수동 확인)
